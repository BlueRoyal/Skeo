name: skeo_example
description: Example Flutter app for the skeo package.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  skeo:
    path: ../
  video_player: ^2.9.2

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
